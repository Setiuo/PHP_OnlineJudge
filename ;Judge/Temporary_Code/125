#include <stdio.h>
#define a1 2
#define d 2

int main()
{
	int n;
	scanf("%d", &n);
	
	int an = a1 + ((n--) - 1) * d;

	int Sn = n * a1 + (int)((1.0 / 2.0) * (n - 1) * n * d);

	printf("%d %d\n", an, Sn);

	return 0;
}