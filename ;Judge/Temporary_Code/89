import sys;

for line in sys.stdin:
    a = line.split();
    a = [int(item) for item in a];

def gcd(a, b):
    if b == 0:
        return a;
    else:
        return gcd(b, a % b);

gbs = a[0] * a[1] / gcd(a[0], a[1]);
print (int(gbs));