#include <stdio.h>
#define a1 2
#define d 2

int main()
{
	long long n;
	scanf("%lld", &n);

	long long an = a1 + ((n--) - 1) * d;

	long long Sn = n * a1 + (long long)((n - 1) * n * d / 2);

	printf("%lld %lld\n", an, Sn);

	return 0;
}