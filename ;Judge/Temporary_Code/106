#include <stdio.h>
#include <string.h>
#define Max(a, b) ((a > b) ? (a) : (b))
int main()
{
	char Str1[100000], Str2[100000];
	scanf("%s %s", Str1, Str2);

	char Ans[100000];
	int len1 = strlen(Str1);
	int len2 = strlen(Str2);
	int MaxLen = Max(len1, len2) - 1;
	Ans[MaxLen + 1] = '\0';

	int JW = 0;

	for (int i = len1 - 1, j = len2 - 1; MaxLen >= 0; i--, j--, MaxLen--)
	{
		int iNum;

		if (i >= 0 && j >= 0)
		{
			iNum = Str1[i] - '0' + Str2[j] - '0' + JW;
		}
		else if (i >= 0)
		{
			iNum = Str1[i] - '0' + JW;
		}
		else
		{
			iNum = Str2[j] - '0' + JW;
		}

		Ans[MaxLen] = iNum % 10 + '0';
		JW = iNum / 10;
	}

	if (JW != 0)
		printf("%d", JW);

	printf("%s", Ans);
	return 0;
}