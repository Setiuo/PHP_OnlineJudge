#include <stdio.h>
#include <string.h>

int main()
{
	char Str[1010];
	bool First = true;

	while (scanf("%s", Str) != EOF)
	{
		int len = strlen(Str);

		if (First)
		{
			First = false;
			printf("%d", len);
		}
		else
		{
			printf(",%d", len);
		}
	}

	return 0;
}