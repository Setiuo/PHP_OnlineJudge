#include <stdio.h>

int main()
{
	int n;
	scanf("%d", &n);

	int AllNum[100][100];

	for (int i = 0; i < n; i++)
	{
		for (int m = 0; m < n; m++)
		{
			scanf("%d", &AllNum[i][m]);
		}
	}

	for (int i = 0; i < n; i++)
	{
		for (int m = n - 1; m >= 0; m--)
		{
			printf("%d ", AllNum[m][i]);
		}

		printf("\n");
	}
	return 0;
}