#include <stdio.h>
#define a1 2
#define d 2

typedef long long ll;

int main()
{
	ll n;
	scanf("%lld", &n);

	ll an = 0;
	ll Sn = 0;

	for (int i = 1; i <= n; i++)
	{
		Sn += an;
		an += 2;
	}

	printf("%lld %lld\n", an, Sn);

	return 0;
}